import React, { Component } from 'react';

export const App = () => {
  return <div></div>;
};

this.setState(state => ({ isOpen: !state.isOpen }));

this.setState(prevValueState => {
  return {
    isOpen: !prevValueState.isOpen,
  };
});
